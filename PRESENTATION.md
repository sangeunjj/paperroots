# 🌱 PaperRoots — 연구 논문 계보 어시스턴트

### 산업인공지능세미나(IME844) 기말 프로젝트 발표자료

> AI 코딩 에이전트(Claude Code)와 협업해, "근간이 되는 논문을 일일이 찾는" 연구자의 반복 문제를 해결하는 웹 서비스를 만들었다.

- **제출자**: (이름 / 학번)
- **GitHub**: https://github.com/<본인계정>/paperroots
- **배포 링크**: https://<배포주소>.vercel.app
- **시연 영상**: https://youtu.be/<영상ID> (일부공개)

---

## 1. 프로젝트 소개

### 프로젝트명
**PaperRoots** — 연구 논문 계보(lineage) 어시스턴트

### 문제 정의
> **"특정 주제의 근간이 되는(foundational) 이전 논문들을 일일이 찾아야 하는 문제를 해결한다."**

연구를 시작할 때 가장 막막한 것은 "이 분야가 어디서 출발해 지금 어디까지 왔는가"를 파악하는 일이다. 구글 스칼라로 논문을 한 편씩 열고, 참고문헌을 거슬러 올라가며 *기초가 되는 논문*을 손으로 추적하는 과정은 번거롭고 오래 걸린다.

### 목표
주제 한 단어만 입력하면,
1. 그 분야의 **현재 트렌드**를 요약하고,
2. **최신 논문**을 보여주며,
3. 무엇보다 그 분야의 **근간이 되는 논문**들을 — 단순 검색이 아니라 **인용 관계 분석**으로 — 찾아 3줄 요약과 링크로 정리한다.

---

## 2. 디자인 씽킹 과정

### 🤝 공감 (Empathize)
- 대학원생/연구자는 새 주제를 파고들 때마다 "이 분야의 고전·필독 논문이 뭔지" 모른 채 헤맨다.
- 구글 스칼라 검색은 **최신·고인용 논문**은 잘 보여주지만, "여러 핵심 논문이 공통으로 딛고 선 토대 논문"은 직접 알려주지 않는다.
- 참고문헌을 한 편씩 클릭해 역추적하는 것은 시간 낭비이자 진입 장벽이다.

### 🎯 문제 정의 (Define)
> "주제 하나로, 그 분야의 트렌드 · 최신 논문 · **근간 논문**을 한 번에 정리받고 싶다."

### 💡 아이디어 발상 (Ideate)
- 핵심 통찰: **근간 논문 = 인용 그래프에서 여러 핵심 논문이 공통으로 인용하는 논문.**
- 즉, 최근 핵심 논문 N편을 모아 그들의 참고문헌을 집계하면, 가장 많이 공통 인용된 논문이 그 분야의 **토대**다.
- 여기에 LLM(Claude)을 더해, 트렌드를 사람이 읽기 좋게 요약하고 각 논문을 3줄로 정리한다.
- 데이터는 인용 그래프를 제공하는 **OpenAlex**(무료·키 불필요)를 사용.

### 🛠️ 프로토타이핑 (Prototype)
- Next.js + Claude API + OpenAlex 로 "동작하는 MVP" 구현 (완벽함보다 동작 우선).
- 신분 선택 → 주제 입력 → 결과(트렌드/근간/최신) 한 화면.

### 🧪 테스트 (Test)
- 실제 주제(LLM, RL, diffusion 등)로 결과를 검증하고, 한계와 개선점을 기록 (→ 5·6장).

---

## 3. 시스템 구조

### 아키텍처 & 데이터 흐름
```
[사용자] 신분 + 주제 + (선택)세부 키워드
   │  POST /api/research
   ▼
[Next.js API Route]  (Vercel 서버리스 함수)
   │
   ├─① OpenAlex 검색 (citation graph)
   │     • 관련성 우선 풀 → 피인용수 재정렬   = 핵심(influential) 논문 25편
   │     • 관련성 우선 풀 → 최신순            = 최신 논문
   │     • 핵심 논문들의 referenced_works 빈도 집계 = 근간 후보 (공통 인용 ≥ 2)
   │
   └─② Claude API (claude-opus-4-8, 구조화 출력 tool use)
         • 분야 트렌드 요약   (신분 맞춤 난이도)
         • 논문별 3줄 요약    (moonlight 스타일)
         • 관련성·신뢰성 판단 (오염/무관 레코드 필터링)
   │
   ▼
[결과 화면] 트렌드 카드 · 근간 논문 · 최신 논문 (각 3줄 요약 + 바로가기)
```

### 기술 스택
| 분야 | 사용 | 선택 이유 |
|---|---|---|
| Frontend/Backend | Next.js 15 (App Router), TypeScript | 한 저장소로 UI+API, Vercel 배포 최적 |
| AI | Claude API `claude-opus-4-8` | 트렌드/요약 품질 + 구조화 출력(tool use)으로 안정적 JSON |
| 논문 데이터 | OpenAlex | 인용 그래프(referenced_works) 제공, 무료·API 키 불필요 |
| 배포 | Vercel | GitHub 연동 자동 배포 |

### 핵심 알고리즘 — 근간 논문 계산
```
1) 주제로 최근 4년 관련 논문을 관련성 순으로 수집 → 피인용수로 재정렬 → 핵심 25편
2) 25편 각각의 참고문헌(referenced_works)을 모아 등장 빈도 집계
3) 2편 이상이 공통 인용한 논문만, 빈도 높은 순으로 정렬 → 근간 후보
4) Claude가 관련성·신뢰성을 판단해 오염 레코드를 제거 → 최종 근간 논문
```

---

## 4. AI 코딩 에이전트 활용 과정 ⭐

> 이 프로젝트의 핵심은 **"AI(Claude Code)를 검색 도구가 아니라 협업하는 개발 파트너로 활용"**한 점이다.

### 사용한 AI 도구
- **Claude Code** (Anthropic CLI 기반 코딩 에이전트) — 설계·구현·디버깅 전 과정
- **Claude API** (`claude-opus-4-8`) — 서비스 자체의 AI 두뇌 (요약·판단)

### AI 활용 방식
- 기능 설계 / 코드 생성 / 디버깅 / 데이터 파이프라인 설계 / 리팩토링 / 문서 작성

### 프롬프트 예시 (실제 진행 흐름)
```
"특정 주제 관련 논문을 찾을 때, 그 분야의 근간이 되는 이전 논문들을
 일일이 찾아야 하는 문제를 해결하고 싶어. 신분과 주제를 입력하면
 트렌드 요약 + 최신 논문 + 관련 논문을 3줄 요약과 링크로 정리해줘."
→ Claude Code가 디자인 씽킹으로 문제를 구체화하고,
   '인용 그래프로 근간 논문을 계산'하는 핵심 아이디어를 제안.

"Next.js + Vercel + Claude API 로, 논문 데이터는 무료 API로."
→ OpenAlex(인용 그래프 보유, 키 불필요) 채택 후 전체 코드 생성.
```

### 🐞 AI 협업 디버깅 사례 (실패 → 해결)

> 과제 취지: "오류와 실패 과정도 훌륭한 프로젝트 경험이다."

**사례 1 — 검색 품질 문제**
- 처음엔 OpenAlex에서 곧장 *피인용수 순*으로 논문을 가져왔다.
- 검증해보니 "large language models" 검색에 **주제와 무관한 고인용 논문**(예: 자동정리증명 논문 "MizAR")이 섞였다.
- Claude Code와 함께 원인을 진단: *피인용 정렬이 관련성을 무시*한다는 것.
- **해결**: "관련성(relevance) 우선으로 후보를 모은 뒤, 그 안에서 피인용수로 재정렬"하도록 변경 → 핵심 논문 풀이 깨끗해짐 (ChatGPT, LLM in medicine, Survey of LLM Evaluation 등).

**사례 2 — 데이터 오염 레코드**
- 인용 그래프로 근간 논문을 계산하니 **LLaMA, InstructGPT(RLHF), T5** 같은 *진짜 근간 논문*이 잘 잡혔다. ✅
- 그러나 OpenAlex 데이터에 섞인 **비정상 레코드**(피인용수가 비현실적으로 부풀려진 스팸성 논문)도 함께 올라왔다.
- 순수 규칙(citation 임계값 등)으로는 거르기 어려웠다 — 진짜 근간 논문도 피인용수가 높기 때문.
- **해결**: 어차피 호출하는 **Claude에게 관련성·신뢰성 판단을 위임**. 각 논문에 `relevant` 플래그를 받아, 주제와 무관하거나 오염된 레코드를 자동 필터링.
- → AI를 "검색"이 아니라 **"판단 파트너"**로 쓴 대표 사례.

**사례 3 — 견고함(robustness)**
- 일부 참고문헌 ID가 OpenAlex에서 404(병합/삭제)로 응답.
- `try/catch`로 개별 실패를 무시하고 후보를 넉넉히(14편) 뽑아, 최종 8편을 안정적으로 확보.

---

## 5. 구현 결과

### 주요 기능
- ✅ 신분(학부생/대학원생/교수/현업) 선택 → 트렌드 요약 난이도 자동 조절
- ✅ 주제 + 세부 키워드 입력
- ✅ **분야 트렌드 요약** 카드
- ✅ **근간 논문** 섹션 — "핵심 논문 N편이 인용" 배지로 인용 그래프 근거 표시
- ✅ **최신 논문** 섹션
- ✅ 각 논문 3줄 요약 + 피인용수 + 클릭 시 원문 바로가기

### 실행 화면
> (스크린샷 삽입: 메인 화면 / 검색 입력 / 결과 — 트렌드·근간·최신)

### 시연 결과 (테스트)
| 테스트 항목 | 결과 |
|---|---|
| OpenAlex 검색 + 근간 계산 응답 속도 | 약 4초 (Claude 요약 제외) |
| 전체 1회 응답 속도 | (측정값 기입) |
| "LLM" 근간 논문 정확도 | LLaMA·InstructGPT·T5 등 실제 근간 논문 포착 ✅ |
| 오염 레코드 필터링 | Claude 판단으로 무관/스팸 레코드 제거 ✅ |
| 트렌드 요약 품질 | (체감 평가 기입) |

---

## 6. 회고 및 개선 방향

### 한계점
- OpenAlex 데이터 품질에 의존 — 일부 분야/레코드는 메타데이터가 부정확.
- 근간 논문은 "최근 핵심 논문의 공통 인용"으로 근사 — 분야 정의가 넓으면 토대가 흐려질 수 있음.
- Claude 호출이 1회당 비용 발생 (모델 환경변수로 조절 가능).

### 추가 아이디어
- 근간 논문 간 **인용 관계를 그래프로 시각화** (계보 트리)
- 결과를 **BibTeX / Markdown으로 내보내기**
- 사용자가 가진 논문 1편을 입력 → 그 논문의 **조상 논문 추적** 모드
- 프롬프트 캐싱으로 반복 검색 비용 절감

### 다음 단계
- Semantic Scholar 등 데이터 소스 다변화로 품질 보강
- 검색 히스토리 / 즐겨찾기
- 분야별 추천(신분·관심사 기반)

---

## 7. 시연 영상 링크

🎬 **YouTube (일부공개)**: https://youtu.be/<영상ID>

영상 구성: ① 해결하려는 문제 → ② 서비스 소개 → ③ 주요 기능 시연 → ④ Claude Code 활용·협업 과정 → ⑤ 어려웠던 점과 개선 방향
